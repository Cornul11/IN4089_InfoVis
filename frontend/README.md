In order to run this, first you need to install the dependencies using the following command:
```shell
npm install live-server -g 
```

Then, in order to run the app, execute:
```shell
live-server .
```

